/*
 * GameDetails sagas
 */

import { take, call, put, select } from 'redux-saga/effects';  // eslint-disable-line no-unused-vars

// All sagas to be loaded
export default [
  defaultSaga,
];

// Individual exports for testing
export function* defaultSaga() {
  //
}
